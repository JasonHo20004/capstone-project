services:
  # 1. PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: capstone-postgres
    environment:
      POSTGRES_DB: capstone_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - capstone-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. Redis Service
  redis:
    image: redis:7-alpine
    container_name: capstone-redis
    ports:
      - "6379:6379"
    networks:
      - capstone-network
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - ./redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 3

  # 3. Backend Service
  backend:
    build:
      context: ./backend  # Chắc chắn code nằm trong thư mục backend
      dockerfile: Dockerfile
    container_name: capstone-backend
    environment:
      # Sửa thành production để code chạy đúng mode
      NODE_ENV: production 
      PORT: 3000
      # Dùng tên service để kết nối
      DATABASE_URL: postgresql://postgres_dev:7Y9Cb8ARpD0UC6lXjNpoEsvgNu89Csnh@dpg-d4r2nhkhg0os738jf5ig-a/capstone_dev_yy4c
      REDIS_URL: redis://redis:6379
      # Thêm các biến khác từ .env vào đây nếu cần
    ports:
      - "3000:3000"
    # Đã XÓA phần volumes để tránh conflict code
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - capstone-network
    # Chạy migrate rồi mới start server
    command: sh -c "npx prisma migrate deploy && node dist/server.js"

volumes:
  postgres_data:
  # Xóa backend_node_modules vì không dùng volume nữa

networks:
  capstone-network:
    driver: bridge